import React from 'react';

const Hotlines = ({numbersInfo}) => {
    const {weblink, hotline, details } = numbersInfo;

    return (
        <div>
            

        </div>
    );
};

export default Hotlines;